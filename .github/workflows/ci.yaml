name: '@fireproof/core'
on:
  push:
    paths-ignore:
      - 'dashboard/*'
jobs:
  compile_test:
    # runs-on: blacksmith-4vcpu-ubuntu-2404
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./actions/base
