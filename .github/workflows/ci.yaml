name: '@fireproof/core'
on:
  push:
    paths-ignore:
      - 'dashboard/*'
jobs:
  compile_test:
    runs-on: blacksmith-4vcpu-ubuntu-2204-arm
    steps:
      - uses: actions/checkout@v4

      - uses: ./actions/base
